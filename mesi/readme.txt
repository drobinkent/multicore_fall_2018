Run  in shell
python3 simulator.py


Thia will run a set of tests defined in wikipedia page. 
This will also describe the steps happening in the simulator.

Then the simulator will generate random.number of test scenarios. 
It will prompt how many instructions you want to generate randomly. 
After you entered an integer, it will generate that number of instructions in random and it will 
simulate the test cases.